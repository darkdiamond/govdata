# govdata-builder Cloud Run service dependencies.
#
# anthropic >= 0.92.0 is required for files.list(scope_id=...) and the
# managed-agents-2026-04-01 beta header. Older versions reject scope_id.
anthropic>=0.92.0
httpx>=0.27.0
pydantic>=2.0
pydantic-settings>=2.0
functions-framework>=3.5
jinja2>=3.1
tenacity>=8.0

# Google Cloud
google-cloud-firestore>=2.16
google-cloud-storage>=2.14
google-cloud-build>=3.25
