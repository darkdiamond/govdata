# Manual-run harness dependencies — NOT installed by the production
# Cloud Run / Cloud Build images. The top-level Dockerfile and
# infra/Dockerfile.publisher only `COPY services/page_builder/requirements.txt`,
# so these stay out of the live builder/publisher.
#
# Local-only install:
#   pip install -r services/page_builder/requirements.txt \
#               -r services/page_builder/requirements-test.txt
#
# Used by:
#   services/page_builder/kimi_runner.py
#   services/page_builder/podman_sandbox.py
#   services/page_builder/cli/kimi_test.py
#   services/page_builder/cli/preview.py

pydantic-ai>=1.91.0,<2       # agent loop; 1.91 carries PR #3137 — fixes Anthropic streaming adapter dropping tool_use args at max_tokens

llm-sandbox[podman]>=0.3     # rootless Podman sandbox; needs `apt install podman`
duckduckgo-search>=8         # free web_search backend, no API key
